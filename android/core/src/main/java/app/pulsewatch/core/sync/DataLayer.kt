package app.pulsewatch.core.sync

/** Wearable Data Layer keys shared by phone and watch. */
object DataLayer {
    const val PATH_SESSION = "/pulsewatch/session"
    const val PATH_PAIRING = "/pulsewatch/pairing"
    const val KEY_PAYLOAD = "payload"
    const val KEY_PAIRING_CODE = "code"
}
